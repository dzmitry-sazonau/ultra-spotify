import { ButtonSize } from './interface'

export const buttonSizeMap: { [key in ButtonSize]: number } = {
  xs: 16,
  s: 32,
  m: 40,
  l: 48,
  xl: 56,
}

export const iconSizeMap: { [key in ButtonSize]: number } = {
  xs: 16,
  s: 22,
  m: 40,
  l: 48,
  xl: 56,
}


